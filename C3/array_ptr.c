#include<stdio.h>

void read(int n,int *p[n]){
for(int i=0;i<n;i++)
{
    scanf("%d",&a1[n]);
   

}
}
int main(){
    int n;
    printf("Enter the number of elements in array");
    int a[n];
    int *ap[n];
    for(int i=0;i<n;i++)
    {
        ap[i]=&a[i];
    }
    read(n,ap);
    write(n,ap);

}